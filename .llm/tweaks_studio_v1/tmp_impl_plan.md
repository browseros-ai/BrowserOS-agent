# Implementation Plan

1. Create a new WXT package at `apps/tweaks`.
2. Add local storage, matching, import/export, capability tagging, and content runtime modules.
3. Build the full-page Studio for list, editor, metadata, and import flows.
4. Build the popup for current-site discovery and quick toggles.
5. Wire install-time starter seeding and Studio launch in the background script.
6. Add root scripts and README updates.
7. Verify with package typecheck, package build, root `build:tweaks`, and Biome checks.
